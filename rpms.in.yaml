contentOrigin:
  repofiles:
    - ./rpm-repos/ubi.repo

packages:
  - util-linux
  - pciutils
  - infiniband-diags
  - libibverbs-utils

arches:
  - x86_64
  - aarch64

context:
  containerfile: Dockerfile.konflux.cluster-validation
